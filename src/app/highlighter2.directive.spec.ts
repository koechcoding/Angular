import { Highlighter2Directive } from './highlighter2.directive';

describe('Highlighter2Directive', () => {
  it('should create an instance', () => {
    const directive = new Highlighter2Directive();
    expect(directive).toBeTruthy();
  });
});
